import React from 'react';
import TodoContainer from './TodoContainer';  // Container Component

const App = () => (
  <div>
    <h1>할 일 목록</h1>
    <TodoContainer />
  </div>
);

export default App;
